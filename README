```
🔑 Wave Key Generator
Un generador automatizado de claves para Wave utilizando Python y Selenium.
📋 Requisitos Previos

Python 3.10.11
Google Chrome o Chromium
ChromeDriver compatible con tu versión de Chrome

⚙️ Configuración del Entorno
1. Variable de Entorno ChromeDriver
Es necesario configurar la variable de entorno que apunta al directorio del ChromeDriver:

Abre las Variables de entorno del sistema
En "Variables de usuario", haz clic en "Nueva"
Nombre: PATH
Valor: C:\Users\TU_USUARIO\chromedriver-win64
Haz clic en "Aceptar" para guardar


⚠️ Importante: Reemplaza TU_USUARIO con tu nombre de usuario de Windows

2. Instalación de Python

Descarga Python 3.10.11 desde el enlace proporcionado
Durante la instalación:

✅ Marca "Add Python to PATH"
Sigue el asistente de instalación



3. Configuración de ChromeDriver
Crear directorio para ChromeDriver
mkdir chromedriver-win64
Descomprime el archivo descargado en esta carpeta
Verifica que chromedriver.exe esté en: chromedriver-win64/chromedriver.exe
4. Instalación de Dependencias
pip install -r requirements.txt
🚀 Uso

Verifica tu conexión a Internet
Ejecuta el script:
python WaveGenerateKey.py

📁 Estructura del Proyecto
wave-key-generator/
│
├── WaveGenerateKey.py    # Script principal
├── requirements.txt      # Dependencias
├── chromedriver-win64/   # Directorio del driver
│   └── chromedriver.exe
├── keys.txt             # Archivo de claves generadas
└── README.md            # Documentación
🌐 Versiones Compatibles de ChromeDriver
Versión de ChromeChromeDriverEnlace de DescargaChrome 130v130.0.6723.91DescargarChrome 129v129.0.6668.89Descargar
🔍 Verificar tu Versión de Chrome

Abre Google Chrome
En la barra de direcciones, escribe: chrome://version
Descarga el ChromeDriver correspondiente a tu versión

🔧 Solución de Problemas
Si encuentras errores de compatibilidad con ChromeDriver:

Verifica tu versión de Chrome (chrome://version)
Descarga el ChromeDriver correspondiente
Reemplaza el archivo existente en la carpeta chromedriver-win64

💡 Consejos

Utiliza una VPN para evitar limitaciones de uso
Las claves generadas se guardan automáticamente en keys.txt
Mantén actualizado tanto Chrome como ChromeDriver
```
